# Copy to .env and fill in to enable the one live AI call in the app
# (Claude summarizing a preparer's correction note for the audit log).
# Without this, the app still works fine -- it just shows the raw note
# with no AI-generated summary underneath it.
ANTHROPIC_API_KEY=
